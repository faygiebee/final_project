import './Amount.css';

function Amount(){
    return (<>
        <h1 className='price'> {number}</h1>
        <span className='price-currency'>({Currency})</span> 
        </>
    );
}

export default Amount;